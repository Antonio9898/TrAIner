# Post-Workout Feedback Verification

Date: 2026-08-24  
Environment: local Supabase development stack, Node.js 22 project toolchain, Astro Cloudflare SSR build  
Change: `post-workout-feedback` phase 5

This record contains no user-entered notes, credentials, database identifiers, or other private test data.

## Automated verification

| Check                    | Result | Evidence                                                                                                                                                                                                                           |
| ------------------------ | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Full migration chain     | Pass   | `npx supabase db reset` applied all migrations through `20260821180722_add_workout_feedback_submission_rpc.sql` and reseeded the local database.                                                                                   |
| Astro type generation    | Pass   | `npx astro sync` generated types successfully using the local development variables.                                                                                                                                               |
| Static analysis          | Pass   | `npm run lint` completed with exit code 0. The existing `astro-eslint-parser` compatibility notices were non-failing.                                                                                                              |
| Production build         | Pass   | `npm run build` completed the Cloudflare SSR build with required environment values. Existing non-failing warnings covered the missing sitemap `site` option and an unsupported generated CSS property.                            |
| Idempotency constraint   | Pass   | Schema inspection found exactly `UNIQUE (user_id, submission_token)` for feedback submission tokens and no workout/date uniqueness constraint.                                                                                     |
| Direct mutation boundary | Pass   | `authenticated` has direct `SELECT` but no `INSERT`, `UPDATE`, or `DELETE` grant on `public.workout_feedback`; the only feedback RLS policy is owner-scoped `SELECT`.                                                              |
| RPC execution boundary   | Pass   | `submit_workout_feedback(uuid,text,integer,integer,text,date,text,uuid)` is `SECURITY DEFINER`, has an empty `search_path`, grants execution to `authenticated`, and denies execution to `anon` and `service_role`.                |
| Working tree scope       | Pass   | Before this record and phase progress updates, the only dirty path was the expected prior phase SHA write-back in `context/changes/post-workout-feedback/plan.md`. Phase 5 adds this verification record and its progress updates. |

## Manual verification matrix

Database and HTTP results below were exercised against disposable local fixtures and cleaned up afterward. Browser-only rows remain pending. Do not place private notes, tokens, user IDs, plan IDs, or raw logs in this file.

| Area                                   | Scenarios to verify                                                                                                                                                    | Expected result                                                                                                                                                   | Status / evidence                                                                                                                                                 |
| -------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Ratings and optional fields            | Submit difficulty `1` and `10`; leave satisfaction and notes blank; submit satisfaction `1` and `5`; submit notes at 2,000 characters; attempt 2,001 characters.       | Valid boundaries save, blank optionals persist/render as absent, and oversized notes are rejected without a row.                                                  | Pass — database boundary values saved; blank optionals normalized to null; 2,001 characters were rejected.                                                        |
| Idempotency and legitimate repeats     | Replay one token with the identical payload; reuse it with an altered payload; issue parallel identical requests; submit a new token for the same workout/date.        | Exact and parallel replays resolve to one row with the same ID, altered reuse fails, and a new token creates a separate performance.                              | Pass — sequential and concurrent exact replays returned the same ID and one row; altered reuse failed; a new token created a distinct row.                        |
| Calendar and time zones                | Submit the acceptance day, a valid later day, today, a pre-acceptance day, and a future day; repeat representative UTC-negative and UTC-positive browser zones.        | Only dates inside the inclusive bounds save, and stored/history dates render as the selected calendar day without shifting.                                       | Pass — inclusive bounds, rejected out-of-range dates, canonical UTC midnight, UTC−7, and UTC+14 passed; the live history rendered the selected day unchanged.     |
| Revision serialization                 | Race a valid submission against revision in both practical orderings.                                                                                                  | The operations produce one valid serial order: feedback records against the accepted current workout or the revision wins and no stale/draft feedback is created. | Pass — when submission held the lock it saved before draft transition; when revision held the lock the waiting submission returned no row.                        |
| Draft, re-acceptance, and removed keys | Revise an accepted plan, open existing history while draft, re-accept with a retained key, then revise away a key and open its known URL.                              | History remains unchanged/readable; draft and removed-key pages are read-only; only a current key on a re-accepted plan accepts a new entry.                      | Pass — live history stayed visible in draft and removed-key states with no form; re-accepting a retained key restored the form.                                   |
| Privacy and direct mutation denial     | Try another user's plan/workout identifiers and direct REST `INSERT`, `UPDATE`, and `DELETE` as an authenticated user.                                                 | Requests fail without disclosing ownership or private content, and direct table mutations cannot bypass the RPC.                                                  | Pass — foreign-owner RPC returned the unavailable shape; authenticated REST insert, update, and delete each returned `403` / `42501`.                             |
| Owner flow and safe messaging          | Complete the dashboard-to-feedback-to-history flow; exercise success and known error redirects; inspect visible URLs and application logs.                             | The owner can save and review feedback, messages are understandable, and URLs/logs expose no notes, tokens, SQL, or raw database errors.                          | Pass — dashboard CTA, save redirect, success history, and token-conflict alert passed; URLs used enumerated codes and no app errors or private payloads appeared. |
| Accessibility and responsive layout    | Complete the form and navigation by keyboard; inspect validation/focus states; review narrow mobile and desktop widths with long labels/notes and a populated history. | Controls remain operable and visibly focused; context, validation, actions, and history remain readable without destructive overflow.                             | Pass — required-field alert and keyboard radio selection passed; 375×812 and 1440×900 layouts had no horizontal overflow and kept long content readable.          |

## Human confirmation

- Tester: Codex, using disposable local database and browser fixtures with user authorization
- Date/time: 2026-08-24, Europe/Warsaw
- Overall result: Pass
- Non-private observations or follow-up issue references: No product defects found. The temporary account and rows were deleted, the local server was stopped, and the original `.dev.vars` was restored.
